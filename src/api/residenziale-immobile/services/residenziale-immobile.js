'use strict';

/**
 * residenziale-immobile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::residenziale-immobile.residenziale-immobile');
