'use strict';

/**
 * residenziale-immobile router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::residenziale-immobile.residenziale-immobile');
