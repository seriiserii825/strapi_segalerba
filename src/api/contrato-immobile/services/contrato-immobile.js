'use strict';

/**
 * contrato-immobile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contrato-immobile.contrato-immobile');
