'use strict';

/**
 * localita-provincia router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::localita-provincia.localita-provincia');
