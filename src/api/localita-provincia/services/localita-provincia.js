'use strict';

/**
 * localita-provincia service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::localita-provincia.localita-provincia');
