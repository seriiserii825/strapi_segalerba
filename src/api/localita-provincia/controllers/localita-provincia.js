'use strict';

/**
 * localita-provincia controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::localita-provincia.localita-provincia');
